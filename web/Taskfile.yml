version: '3'

tasks:
  install:
    desc: Install npm dependencies
    cmds:
      - npm install

  build:
    desc: Build for production
    cmds:
      - npm run build

  dev:
    desc: Start dev server
    cmds:
      - npm run dev

  test:
    desc: Run tests
    cmds:
      - npm run test

  format:
    desc: Run formatter
    cmds:
      - npm run format

  lint:
    desc: Run biome linter
    cmds:
      - npm run lint

  check:
    desc: Run check
    cmds:
      - npm run check
